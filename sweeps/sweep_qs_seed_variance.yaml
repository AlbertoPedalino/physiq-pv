program: sweeps/sweep_qs_seed_agent.py
method: grid
metric:
  name: best_val_loss
  goal: minimize

# Offline training sweep: only the seed changes.
# Fixed in the agent: BILSTM_POOLING=last, QS_LOSS_WEIGHTING=1, QS_LOSS_FLOOR=0.2.
parameters:
  seed:
    values: [42, 123, 2024, 7, 31337, 2718]
